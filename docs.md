💡 코드 포인트 설명
정규표현식 (re.DOTALL): 리디북스 소스 코드에서 var book 변수는 여러 줄에 걸쳐 작성되어 있습니다. .이 줄바꿈 문자까지 포함하도록 re.DOTALL 옵션을 주는 것이 핵심입니다.

User-Agent 설정: 리디 서버가 봇(Bot)으로 인식하고 차단하는 것을 방지하기 위해 일반 브라우저인 것처럼 헤더를 설정했습니다.

JSON 데이터 구조: 목록 API에서 받은 제목과 상세 페이지에서 추출한 '종료 날짜'를 하나의 객체로 합쳤습니다. 이제 이 객체를 DB에 넣기만 하면 됩니다.

docker-compose up -d